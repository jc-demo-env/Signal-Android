plugins {
    id 'signal-sample-app'
}

android {
    namespace 'org.signal.contactstest'

    defaultConfig {
        applicationId "org.signal.contactstest.jc1.test"
    }
}

dependencies {
    implementation project(':contacts')
}
